<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SiqesNativeApiCall" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SiqesNativeApiCall" parent="Base.Theme.SiqesNativeApiCall" />

    <!-- Custom text appearance -->
    <style name="CustomFont">
        <item name="android:fontFamily">@font/dancing_script</item>
        <!-- Add other text appearance attributes as needed -->
    </style>
    <style name="CustomAuthorFont">
        <item name="android:fontFamily">@font/roboto_regular</item>
        <!-- Add other text appearance attributes as needed -->
    </style>
</resources>